{
  "schema": "afterbell-agent-service/1",
  "name": "AfterBell Watchtower",
  "version": "0.1.0",
  "description": "Continuously evaluates whether a tokenized-stock position preserves the owner's economic exposure and rights, then returns a policy-bound rescue signal.",
  "status": "DEPLOYED_TESTNET_TRIAL",
  "truthNotice": "The official A2A/X402 seller runtime is deployed in the BNB Agent Studio managed BSC Testnet trial. The local HTTP manifest remains an integration reference; paid ERC-8183 delivery and B402 settlement are not claimed.",
  "http": {
    "method": "POST",
    "path": "/api/v1/agent/watchtower"
  },
  "input": "WatchedPosition",
  "output": "WatchtowerEvent",
  "billing": {
    "model": "per-inspection",
    "currency": "USDT",
    "price": "0.01"
  }
}
